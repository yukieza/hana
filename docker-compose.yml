services:
  kaiwa:
    build: .
    ports:
      - "8000:8000"
    environment:
      - VOICEVOX_URL=http://192.168.101.3:50021
    volumes:
      - ./key.txt:/app/key.txt:ro
      - ./logs:/app/logs
    restart: unless-stopped
